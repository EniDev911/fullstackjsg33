let languages = ["JavaScript", "C#", "PHP", "Java", "Python"];

languages.forEach((valor, index, arr) => {
  console.log(`${valor} se encuentra en el indice ${index} dentro del arreglo [${arr}]
  `);
});
